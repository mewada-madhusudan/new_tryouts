I have a PyQt6 desktop app with a grid of application cards (likely built with QGridLayout or a custom flow layout). Each card is a widget showing title, category tag, status badge, description, and install/uninstall buttons.

Each card supports a "Show Details" flip interaction — clicking/right-clicking should flip the card in place to reveal a back face with metadata (IAHub ID, Expiry Date, Version, Developed By), then flip back.

Bug: The current flip implementation is not contained within the card's own widget bounds — when a card flips, it visually overlaps/bleeds into neighboring cards in the grid instead of staying clipped inside its own cell. The surrounding grid layout should never shift, resize, or be affected.

Please:

Find the widget class responsible for rendering the application cards and the code handling the "Show Details" flip animation.
Diagnose why the flip animation is escaping the card's bounds — check for: animations targeting geometry()/resize() instead of an internal transform, missing setFixedSize() on the card container, missing clipping (e.g. no QWidget.setAttribute(Qt.WA_StyledBackground), no setMask(), or the animated child widget not being a proper child of the fixed-size container), or z-order/stacking issues with raise_().
Refactor the flip so it's fully self-contained: wrap front and back faces in a QStackedWidget (or QStackedLayout) inside a single fixed-size container widget, and animate the transition (e.g. scale/opacity via QPropertyAnimation) without ever changing the container's own size or position in the parent QGridLayout.
Make sure the fix keeps the grid layout completely stable — no reflow of sibling cards during or after the flip, in any window size.
Preserve existing styling (rounded corners, shadows, spacing) on both front and back faces.
Show me the diff/changes needed, and explain what was causing the overlap
